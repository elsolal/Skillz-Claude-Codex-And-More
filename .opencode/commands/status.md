---
description: Project status dashboard
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL `.opencode/skills/status-workflow/SKILL.md` if it exists, otherwise `.claude/skills/status-workflow/SKILL.md`, READ its entire contents, and execute the read-only scan.

This is a READ-ONLY workflow. Never modify files, never commit, never push. Scan the data sources, present the 4-section dashboard (Planning Checklist, GitHub Sync, Git State, Recommendations), and finish with a single `Next step: <command>` line based on the project state.

If any data source is missing, mark it as `-` in the output and continue.
