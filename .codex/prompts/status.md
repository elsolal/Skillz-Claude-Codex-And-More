---
description: 'Project status dashboard — planning docs, GitHub issues, git state, and next-step recommendation'
disable-model-invocation: true
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL `~/.codex/skills/status-workflow/SKILL.md`, READ its entire contents, and execute the read-only scan.

This is a READ-ONLY workflow. Never modify files, never commit, never push. Just scan the data sources, present the 4-section dashboard (Planning Checklist, GitHub Sync, Git State, Recommendations), and finish with a single `Next step: <command>` line based on the project state.

If any data source is missing (directory doesn't exist, no GitHub issues, etc.), mark it as "—" in the output — do not fail the whole command.
